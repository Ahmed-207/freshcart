import { ProductCutPipe } from './product-cut-pipe';

describe('ProductCutPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCutPipe();
    expect(pipe).toBeTruthy();
  });
});
